<div class="col-lg-3 col-md-4 col-sm-6">
  <div class="panel panel-default subscription-panel <%="subscribed" if current_user.subscribed?(topic) %>">
    <div class="panel-heading">
      <h4 class="panel-title"><%= topic.name.camelize %></h4>
    </div>
    <div class="panel-body">
      <%= image_tag topic.picture.url, class: "img-responsive" if topic.picture? %>
      <h4>Brought to you by <%= link_to topic.author, topic.link, target: '_blank' %></h4>
    </div>
  </div>
</div>