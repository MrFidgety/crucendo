<div class="crucial-container">
  Interaction <%= @interaction.id %> - started on <%= @interaction.created_at %><br><br>
  <% wizard_steps.each do |s| %>
    <%= '>' if step.equal? s %>
    <%= s %><br>
  <% end %>
</div>